# Third-party source materials (copyright) + large media — never publish
research/research-materials/
research/extracted/

# Verbatim model dumps (large; digested versions live in research/adversarial-review.md)
research/raw-model-output/

# Internal peer-session pin (not useful publicly)
.astra-session

# test-run/ IS committed (official A/B efficacy evidence), but never commit
# the working skill clones or raw model run logs inside it:
test-run/skill-clone/
test-run/**/humanscope-skill/
test-run/**/*.log

# OS / misc
.DS_Store
Thumbs.db

# Local Codex per-project pin (personal setup; not for the public repo)
.codex/

# Third-party competitor skill (Chen Media, no license) — download from https://chen.media/downloads/human-scope.zip
test-run/fair-retest/_competitor/
